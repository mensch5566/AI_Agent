1. `P1` The v2 fold for item 1 is still wrong as written: routing IS/CF through the existing dictionary builder does **not** sidestep the `METRIC_ONLY_UNI` trap. `buildDictionaryMatrix()` never checks `METRIC_ONLY_UNI` and blindly renders whatever rows exist in `ROWS_BY_STATEMENT` ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:361), [constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:29)). `IS_ROWS` still includes `ebitda` and `CF_ROWS` still includes `free_cash_flow` ([constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:54), [constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:119)), while the current PDF path explicitly suppresses those keys via `METRIC_ONLY_UNI` ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:117), [useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:324)). So the spec claim in [the spec](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/superpowers/specs/2026-06-08-statement-view-mode-toggle-design.md:52) is mis-applied.

2. `P2` The localStorage fold is incomplete. An effect-read is hydration-safe, but it guarantees a first paint in `'pdf'` before a stored `'uni'` can apply, because the matrix is rendered immediately from state in [Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:41>) and the table/chart render synchronously from that memoized matrix ([Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:237>)). Also, if the persist effect is unguarded, the initial `'pdf'` state can be written back before the mount-read update commits. v2 needs an explicit “storage has been read” guard for persistence, and probably a mount gate if you want to avoid flash-of-wrong-mode.

3. `P3` The toggle-scope reasoning is slightly overstated. Hiding the control on `SEGMENT` is fine for UX, but `SEGMENT` still aliases `statement` to `"IS"` and computes IS matrices in the background ([Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:39>), [Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:41>)). So “no code path applies viewMode on SEGMENT” is not literally true; it’s just not user-visible.

Fold-by-fold:
1. `Incorrect overall.` `buildDictionaryMatrix(filtered, statement, periods)` is structurally generic enough for IS/BS/CF, and `ROWS_BY_STATEMENT[IS/BS/CF]` does exist ([constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:159)). It does **not** depend on RATIO-specific period logic. But the fold is still wrong because it would resurrect inline `ebitda` / `free_cash_flow`.
2. `Correct.` Setting `display_negated: null` on the synthetic SUM cell is the right fix for the sign bug ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:411), [StatementMatrix.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/StatementMatrix.tsx:55)). I do not see another stale spread field that corrupts uni rendering: `row.label` comes from the matrix row definition, not `cell.display_label` ([StatementMatrix.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/StatementMatrix.tsx:262)).
3. `Correct.` Extending the existing statement-reset effect to also depend on `viewMode` is the right mechanism ([Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:99>)). It should not loop, because it only writes `selectedKeys`, not `statement` or `viewMode`.
4. `Mostly correct.` Hiding the toggle outside IS/BS/CF is sufficient from the user’s perspective ([Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:194>), [Viewer.tsx](</Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:237>)). The only correction is the spec wording about SEGMENT’s code path.
5. `Incorrect/incomplete.` Hydration-safe, yes; no-flash/no-overwrite, no. v2 needs an initialization guard.

Verdict: v2 is **not** ready for design-gate approval yet; the `buildDictionaryMatrix` / `METRIC_ONLY_UNI` fold and the localStorage initialization design are remaining blockers.