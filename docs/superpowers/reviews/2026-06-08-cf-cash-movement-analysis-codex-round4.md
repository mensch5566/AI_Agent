Remaining blocker:

1. `P2` The scope reduction is logically sound, but the spec is not internally converged yet because it still reintroduces an FX canonical-row requirement in the placement summary. Section C correctly scopes the FX display row out (`docs/superpowers/specs/2026-06-08-cf-cash-movement-analysis-design.md:82`-`86`), and Out of scope repeats that (`...:143`-`144`). But the layer summary still says canonical rows are `CF_ROWS` with an “approved fx key row + frontend-only cash_beginning_of_period presentation row” (`...:111`-`112`). That is the same governance issue round 3 flagged, now only as a stale spec contradiction. Until that line is removed or rewritten, the design is not fully converged because an implementer could still read the summary and add the FX row anyway.

On your three checks:
1. Scoping out the FX display row does leave the cash begin/end fix itself complete and correct. The core design is adapter relabel for the stored ending fact plus a pre-builder movement-analysis helper that synthesizes beginning from the predecessor ending fact; neither requires FX to be a displayed CF row (`docs/superpowers/specs/2026-06-08-cf-cash-movement-analysis-design.md:31`-`69`).
2. The footing check can still read `fx_effect` without an FX display row. The spec explicitly places the helper before either builder and says it consumes raw filtered `Cell[]` (`...:50`-`69`). The code shape supports that: `buildMatrix()` computes `filtered` before the `viewMode` split (`app/components/financials-v2/useFinancialMatrix.ts:193`-`248`), and `CF_ROWS` gating only happens inside `buildDictionaryMatrix()` later (`...:369`-`398`). So a pre-builder helper can inspect an `fx_effect` fact from `filtered` even though uni-mode would never render it.
3. No new behavioral/accounting flaw is introduced by removing the FX display row. The footing identity remains concept-aware and valid as written: `Including` uses `end - begin == net_change`, `Excluding` uses `end - begin == net_change + fx_effect` (`docs/superpowers/specs/2026-06-08-cf-cash-movement-analysis-design.md:89`-`97`). The only new issue is the stale contradictory summary line above.

Verdict: remaining blocker at [docs/superpowers/specs/2026-06-08-cf-cash-movement-analysis-design.md:111](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/superpowers/specs/2026-06-08-cf-cash-movement-analysis-design.md:111). After that summary is brought into sync with Sections C and Out of scope, I would call it `CONVERGED`.