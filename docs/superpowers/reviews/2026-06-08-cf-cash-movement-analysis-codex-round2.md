**Findings**

1. **P2 — the v2 row-key plan still violates the current schema contract and will double ending-cash unless it is tightened.**  
The SEC v2 dictionary already has `ending_cash` as the approved key, and the governance docs require any new/changed `uni_account` to be registered first ([docs/financials-view-schema.md](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/financials-view-schema.md:16), [docs/financials-view-schema.md](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/financials-view-schema.md:48), [docs/sec-financials-v2-schema.md](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/sec-financials-v2-schema.md:226), [docs/sec-financials-v2-schema.md](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/docs/sec-financials-v2-schema.md:227), [app/components/financials-v2/constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:125), [app/components/financials-v2/constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:126)). `buildDictionaryMatrix()` also populates rows by `c.uni_account`, so a direct `ending_cash` fact will still land on `ending_cash`; a new `cash_end_of_period` row will stay empty unless you special-case projection ([app/components/financials-v2/useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:390), [app/components/financials-v2/useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:397)).  
Use `ending_cash` for the disclosed end row, and either reuse the approved FX key family or declare the extra row keys as frontend-only row ids, not new `uni_account`s.

2. **P2 — the `sortOrdinal` implementation target is still off by layer.**  
The matrix row shape is defined locally in `useFinancialMatrix.ts`, not in `types.ts` ([app/components/financials-v2/useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:39)). `StatementMatrix` does not sort rows; it renders the builder output in order ([app/components/financials-v2/StatementMatrix.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/StatementMatrix.tsx:262)). Current ordering happens inside the builders ([app/components/financials-v2/useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:301), [app/components/financials-v2/useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:465)).  
So the design is directionally right, but the minimal change belongs in `useFinancialMatrix.ts`, not `types.ts`/`StatementMatrix.tsx`.

**Per-question judgment**

1. **Adapter relabel:** fold is correct. Selecting the period-end arc is compatible with `display_negated` because negation is derived from the same matched presentation edge ([presentation_resolver.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/Tools/research-tools/_shared/presentation_resolver.py:185), [presentation_resolver.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/Tools/research-tools/_shared/presentation_resolver.py:202)). The coverage gate behavior is unchanged: unresolved eligible GAAP facts still block apply ([sec_json_adapter.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/Tools/research-tools/_shared/sec_json_adapter.py:672), [scripts/upsert_sec_financials.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/scripts/upsert_sec_financials.py:524)). I cannot prove from this codebase that `periodEndLabel` exists for every filer; fail-closed is the right behavior if it does not.

2. **Pre-builder helper / clean call site:** fold is correct. Filtering/remap is currently inside `buildMatrix()` only ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:184)). The clean shared call site is after `filtered`/`periods` are computed and before the `viewMode` split ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:230), [useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:246)). `buildDictionaryMatrix()` alone cannot see the raw filtered cells unless you pass the model in.

3. **Predecessor-by-name:** fold is correct. There is no existing predecessor helper; only a sorter exists ([constants.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/constants.ts:346)). A dedicated helper must be written for `Q2->Q1`, `Q1->Q4 of prior FY`, `FY->prior FY`. The 52/53-week issue is upstream of this layer; the viewer only sees normalized fiscal labels, so exact label predecessor logic is fine once period labels are already correct.

4. **`CF_ROWS` additions:** partially correct, but this is where the remaining P2 sits. PDF mode is unaffected because PDF rows do not use `CF_ROWS` ([useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:243), [useFinancialMatrix.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/useFinancialMatrix.ts:250)). RATIO and `METRIC_ONLY_UNI` are unaffected. The risk is specifically the new row-key naming: `cash_end_of_period` would duplicate the existing `ending_cash` contract unless projection explicitly remaps it.

5. **`sortOrdinal` row model:** directionally correct, but the exact fold is not. The change should be on the `Matrix.rows` shape in `useFinancialMatrix.ts`, and sorting should stay in the builders. `StatementMatrix` is the wrong layer to own ordering. This is a P2 design-precision issue, not a conceptual blocker.

6. **Concept-aware footing:** partially correct. The frontend does receive `xbrl_tag` / `source_account` for fact rows ([types.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/types.ts:29), [app/api/financials/[ticker]/route.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/api/financials/[ticker]/route.ts:97)). But metrics rows have those fields nulled out ([app/api/financials/[ticker]/route.ts](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/api/financials/[ticker]/route.ts:149)). So a direct-fact period can do concept-aware footing in the frontend; a derived `net_change_in_cash` quarter cannot. That is a **P3** unless you intend the footing check to cover reconstructed quarters too, in which case it belongs in parse/test land.

7. **Re-upsert after adapter relabel:** fold is correct. `facts_cell_id()` does not include `display_label` or `ordinal`, so relabeling will not churn identities ([cell_id.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/Tools/research-tools/_shared/cell_id.py:43)). `row_to_dict()` preserves `display_label` / `display_negated` / `ordinal` and strips only `display_eligible` ([scripts/upsert_sec_financials.py](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/scripts/upsert_sec_financials.py:725)). Freshness and coverage gates are unchanged structurally.

8. **Synthetic beginning row vs chart / overlay / `rowsWithData`:** no substantive blocker. `rowsWithData` only checks whether a matrix row has any populated cell ([Viewer.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:79)). `MatrixChart` also consumes matrix rows, not DB row identities ([MatrixChart.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/MatrixChart.tsx:35), [MatrixChart.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/components/financials-v2/MatrixChart.tsx:110)). Non-GAAP overlay is only enabled on IS, not CF ([Viewer.tsx](/Users/mensch5566/AI_Agent/.claude/worktrees/statement-view-pdf-faithful/app/financials/[ticker]/Viewer.tsx:61)). So a frontend-only CF beginning row does not break those consumers.

**Verdict**

No new **P1** remains.  
There are still **2 substantive P2s**, so this is **not yet CONVERGED**:

1. Row-key/schema contract for `cash_end_of_period` / `fx_effect` vs existing `ending_cash`.
2. `sortOrdinal` is assigned to the wrong layer/files in the design.

If those are corrected, I do not see another remaining P1/P2 blocker in the current fold.