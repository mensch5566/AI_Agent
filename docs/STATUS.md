# AI_Agent Status

Updated: 2026-07-03

## Project Profile (SOP §5)

> 依 `~/Obsidian/SOPs/Development/Software Development SOP.md` 填。換 stack 只改這段。

- Project: AI_Agent（個股研究 / Financials Viewer + portal + news/data pipelines）
- **Tier: T3**（financials 資料管道核心 = 100% 精準 / 對外 / 不可逆 → 全套 hard gate；portal/週報等較輕功能視情況 T2）
- SSOT: repo `/Users/mensch5566/AI_Agent`；狀態權威 `docs/STATUS.md`；schema 權威 `docs/financials-core-checklist.md`（LOCKED v5，90 核心 uni_account）+ `financials-architecture.md`
- Stack: Next.js App Router（前端）+ Python skills（parse-10QK-gaap / derive-base / derive-analytics）+ Supabase（`sec_financial_facts` / `_metrics` / `_dimensional_facts`）
- 套件 + lockfile: `package-lock.json`（前端）+ `uv`（Python skills，`uv run --with`）
- 測試: vitest + `tsc --noEmit`（前端）/ pytest（skills）/ Hypothesis property（derive-analytics 對抗式）
- 上 production 機制: parse → build_separated → cross-check → derive-base → derive-analytics → `scripts/upsert_sec_financials.py`（dry-run diff → `--apply`）
- 回滾: git revert + re-upsert known-good（無 schema migration）
- Reviewer: 人類最後把關 + Codex functional review（對抗審查者）；NLM「Topic - Project Dev」當方法論對照
- 資料管道: 統一管道寫 Supabase（禁直接 INSERT/UPDATE）；source（facts）vs derived（metrics RATIO）分離 + provenance/cell_id lineage；dual-key + 12 long-tail bucket
- 觀測（T3 應有，目前**弱 = 已知缺口**）: 待補 pipeline log/告警 + synthetic 驗業務關鍵路徑
- AI 分工: Claude=實作者；Codex=對抗審查者；跨 session 交接用 AI-Memory(memsearch) + `docs/STATUS.md` + `tmp/*-review.md`
- 對外契約: schema = core-checklist LOCKED v5；新 metric 走 checklist 登記流程（不自由創造 uni_account）

## Current Focus
- Portal remains the main entrypoint for internal tools.
- `Financials Viewer` now includes a `Valuation` tab for historical TTM P/E with `日 / 月 / 季` switching.
- Weekly research workflow is being standardized through reusable skills such as `macro-weekly-news`.
- TWSE XBRL ingestion now also supports onboarding new tickers such as `7769 鴻勁` into `Financials Viewer`.
- **parse-SEC-supplement period-anchor guardrail (MVP, 2026-06-12)** shipped: an independent, deterministic, zero-NLM, zero-derive check that catches "wrong-but-present" fiscal-period mislabels (SNDK-type quarter shifts that `coverage_gaps` misses). 13-week anchor off each 10-K `reportDate` (= true FYE, 52/53-week-safe); `{ticker}_period_anchor_validation.json` emitted by `parse_instance_xbrl.py`; `scripts/upsert_sec_financials.py` withholds the whole dimensional batch on a Tier-1 anomaly / missing artifact / zero-anchored-window (fail-closed, `--allow-period-anomaly` override; existing rows preserved). 2-round Opus 4.8 + GPT 5.5 review; 13 TDD; 5 tickers real tier1=0; full dual-source value cross-check deferred to Phase 2 (spec `docs/superpowers/specs/2026-06-12-supplement-stage-b-always-design.md`).
- **Audit Metadata Schema v4 contract shipped end-to-end** across all parse paths (10QK GAAP / 8K Non-GAAP / SEC-supplement v3) → adapter (with dedupe-safe v4 channel merge) → upsert → derive-base, plus an ad-hoc edit CLI (`scripts/manual_edit.py`) for one-off cell corrections and a one-shot DB legacy migration script (`scripts/migrate_db_audit_source_v4.py`). Frontend `Financials Viewer` cells now render audit / classification indicator badges (✓ / ·) with tooltip showing audit_source, note, evidence, preservation event. 258 regression tests cover the contract; shared preservation helper (`_shared/preservation.py`) DRYs the three parse skills' re-extract matrix into one tested module.

## Stable Areas
- `app/page.tsx` portal card layout is the top-level navigation surface.
- `app/financials/` is the main equity fundamentals workspace.
- `macro-weekly-news` workflow and related guide logic have been externalized into global skill config rather than kept only in-session.

## In Progress
- Historical valuation data currently comes from Yahoo price history + quarterly EPS reconstruction.
- Some newer tickers may not yet have enough quarterly EPS history to compute `TTM P/E`; these should render a readable empty state instead of a hard failure.
- Financials and valuation data still come from different sources and are not yet unified behind one data contract.
- TWSE XBRL onboarding still lacks automatic supplement ingestion; `financial_supplement` rows such as weighted-average shares are currently patched in separately when needed.

## Known Gaps
- No formal app-level product spec or module ownership map yet.
- No normalized status/update discipline existed before this file.
- No dedicated regression test coverage yet for the new valuation data flow.

## Development Rules Going Forward
- Update this file after any meaningful feature, data-source, or workflow change.
- For user-facing features, record:
  - what shipped
  - what data source it depends on
  - what is still incomplete or risky
- Prefer adding new portal-facing features behind an existing module/page when possible, instead of creating duplicate entrypoints.

## Latest Changes
- **✅ derive-A：Taiwan derive 併入共用引擎，`twse-derive` 退役** (2026-07-03). 台股（TWSE iXBRL）單季 IS/CF 還原 + ratio 指標，不再靠獨立的 `twse-derive` skill 另開一套邏輯，改與美股共用同一套 `derive-base` / `derive-analytics` bounded 3-pass engine，經新增的 `--market tw` flag 驅動：facts 經 `Tools/research-tools/_shared/twse_json_adapter.py` 轉入引擎；引擎參數化為 TWD-aware（`tolerance.py` 新增 `TWD_thousands`/`TWD_per_share` 容差、`_MONEY_SCALE` 新增 `TWD_thousands`、per-share 輸出 unit 改貨幣感知）；新增 identity allowlist 規則 `IDENTITY_DA_DEP_PLUS_AMORT`（D&A = 折舊+攤銷，對應台股 CF 分列揭露）。台股輸出落在 `MOPS Filings/Skill_Output/derive-{base,analytics}/`。前置：`parse-twse-ixbrl` 的 `uni_account` 全表改名為 US canonical、cr（合併報表）NI/Equity 家族拆分為 `net_income_total_pre_nci`/`net_income`/`net_income_nci` 與 `total_equity_incl_nci`/`total_equity`/`minority_interest_bs`、`xbrl_concept` 現在會填值、`footing_check.py` 的 BS 恆等式改 report_category-aware。三道 Gate 全通過：**Gate 1**（rename 不改變任何數值，純改名）、**Gate 2**（台股走新引擎重現 `twse-derive` 舊指標數值；合併報表 net_income 口徑因家族拆分而有預期內差異，已對帳說明）、**Gate 3**（美股 zero-regression，新舊輸出 byte-identical）。舊 `twse-derive` skill 已從 `CC_Switch_Config` 刪除、manifest 除名；舊輸出 `Skill_Output/twse-derive/{3081,2308}_twse_metrics.json` 保留作歷史對照基準（在 Obsidian vault，非 git-tracked）。Spec：`docs/superpowers/specs/2026-07-02-derive-market-agnostic-design.md`。
- **✅ 品質殘留收尾 + SNDK supplement 期別修正** (2026-06-12). **SNDK 查證結案**：op_income 4111M/rev 5950M（69%）是 filer 真實揭露（IS 完整對帳，記憶體超級循環 + 存貨 writedown reversal），不是抽錯 —— 唯一 bug 是 52/53-週財曆期別標籤錯位（2026-04-03 被標 Q2 應 Q3_FY2026）。**修**：(1) `parse_instance_xbrl.dei_period_label()` 改用 filer 的 dei DocumentFiscalYearFocus+FiscalPeriodFocus（權威、52/53-週安全，全 6 filing 季別對上 flat GAAP）；(2) upsert dimensional 改 **per-ticker 快照替換**（清孤兒，補 design §7 架構缺口，guard 防誤刪、無 FK cascade）。SNDK re-parse + re-upsert（user 授權）：60→60 無孤兒、business_segment Q1-Q3_FY2026 全對。15 period-label tests。**P3：window-gate ✅**（EL2 視窗長度合理性閘 `_window_days_sane`，抓財年變更/stub 錯錨，~340-400 天 band，全 5 ticker drop 0 rows、純 robustness、137 canonical + 2 gate tests）；**ADR-001 layout ✅**（derive-base 測試從 git-ignored flat prototype 遷入 canonical `tests/`，86 tests，conftest 接 scripts/+_shared，prototype 標 DEPRECATED）；**supplement Stage-B-always = 獨立 spawned 工單**（設計變更 + 其他 ticker NLM 重驗，需 work-profile NLM）。commits: parse-SEC-supplement dei + test_period_label；AI `13e9667`(dimensional snapshot-replace)；canonical `7a9041c`(window-gate) + `208a183`(ADR-001 layout)。
- **✅ derive-analytics — BVPS + Segment Operating Margin: SHIPPED to production + front/back verified** (2026-06-11). Production re-upsert of all 5 tickers (user-authorized): BVPS in `sec_financial_metrics` (AAOI 13/INTC 3/LITE 27/MU 22/SNDK 3), Segment margins in `sec_financial_dimensional_facts` (INTC 15/MU 110/SNDK 0/LITE 0). synthetic check all-green (no regression). **Frontend verified**: AAOI Ratios → "Book Value / Share" $5.94…$4.73 (scale-guard); INTC Segment/Geo → "Operating Margin %" Intel Foundry -57.9% / Intel Products 25.9% / Other 7.4% (matches DB, negatives correct, no margin-summing). This completes the /goal "finish remaining derive-analytics metrics" (Adj EBITDA Margin + BVPS + Segment Operating Margin, each Opus 4.8 + GPT 5.5 converged). Details ↓.
- **🔧→✅ derive-analytics — BVPS + Segment Operating Margin: BUILT + dual-model SAFE TO SHIP** (2026-06-11). **BVPS** (`total_equity / common_shares_outstanding → USD_per_share`): per-share scale algebra (equity $-scale ÷ shares count-scale, each→base unit, fail-closed on unknown; AAOI thousands÷millions verified $5.94 not 1000×). Parse promotes `common_shares_outstanding` (us-gaap:CommonStockSharesOutstanding, BS-date instant, `millions_shares`, excluded from BS footing) to a core BS-metadata key. 5 tickers re-parsed: existing BS values byte-identical, coverage 100%, bvps AAOI $5.94/INTC $22.18/LITE $20.23/MU $35.73/SNDK $63.82. Frontend RATIO_ROWS `bvps`. Opus+GPT SAFE TO SHIP (2 non-blocking known limits: preferred-stock gap, dual-class shares). **Segment Operating Margin** (EL3 dimensional, `segment.operating_income / segment.revenue` per business_segment member): new `derive_dimensional_analytics` path writes to `sec_financial_dimensional_facts` on its own rule_id (provenance.derived=true), never the raw-segment or flat-metrics scope. `adapt_dimensional_analytics_facts` stores the raw fraction (bypasses normalize_pct_value — a <-100% segment margin would be corrupted). **Dual-model round-1 caught 2 T3-fatal blockers** (both fixed + re-reviewed SAFE TO SHIP): (1) `_pair_key` null-qname member collapse — MU's all-null-qname members collapsed to one bucket, dropping 88/110 margins + risk of cross-member mispairing; fixed via `_member_id` label fallback + value-conflict fail-closed; (2) SNDK single 'Reportable Segment' false 69.1% margin (consolidated ~8%, upstream supplement mislabel) — suppressed single-member tickers + filed upstream re-verify task. Result: INTC 15 / MU 110 / SNDK 0 / LITE 0 margins. Frontend SegmentDashboard auto-surfaces operating_margin_pct (% format, no margin-summing). **Both metrics await user production-write authorization** (the only remaining step) + post-write frontend visual verification. Commits: canonical `758ffa6`/`7994636`/`886bb39`/`ac9372e`/`3fd2fea`; AI_Agent `7dbf402`/`7360792` + frontend/docs.
- **✅ derive-analytics — Adjusted EBITDA Margin (cross-version) SHIPPED to production** (2026-06-11). First cross-version analytics rule: `adjusted_ebitda_margin_pct = adjusted_ebitda@NON_GAAP / revenue@GAAP`, emitted once under NON_GAAP, basis `MGMT_NONGAAP_INPUT_DERIVED_RATIO` (numerator is a management 8-K disclosure, not GAAP-derived). Engine gained `Term.version` + `AnalyticsRule.output_version`/`allow_cross_version`/`versions` allowlist + `_validate_rules` fail-fast. **Latent bug found+fixed**: io_loader built the Non-GAAP `period_end` map from the 8-K JSON's own metadata (which has none) → EVERY Non-GAAP fact was being rejected; derive-analytics had never loaded one. Now reuses the GAAP period_end map. Version gating (rules_ratios `versions=('GAAP',)` default + rules_crossperiod GAAP-only indexers) keeps the io_loader fix from auto-minting unreviewed NON_GAAP variants → surgical: only +8 NON_GAAP rows (AAOI; Q4 skipped — GAAP revenue is derived_q4 vs adjusted_ebitda quarter_duration). Frontend: `NONGAAP_RATIO_ROWS` overlay surfaces the NON_GAAP cells in the GAAP Ratios grid (fail-closed allowlist). **Dual-model code review (Opus 4.8 + GPT 5.5) both SAFE TO SHIP** — Opus empirically proved 278 GAAP rows byte-identical with/without nongaap loaded (zero regression). 133 canonical tests + 26 vitest + tsc green; AAOI re-upserted (user-auth); browser-verified (Ratios shows (32.0%)…0.6%). Coverage: AAOI-only (only issuer with an adjusted_ebitda disclosure). Commits: canonical `3d43d9c`+harden; AI_Agent `7dbf402` (frontend) + schema/fallback. **Part of /goal "finish remaining derive-analytics metrics"; next: BVPS, then Segment Operating Margin. Book-to-bill DO NOT BUILD (no XBRL order data).**
- **✅ PDF-faithful "As Reported" statement view + whole-row dedup — SHIPPED to production + MERGED to main** (2026-06-11, merge commit `6d901d4`). The full PDF-faithful statement view is live: backend resolves per-fact display metadata (`display_label` / `ordinal` / `display_negated`) from the XBRL presentation linkbase behind a fail-closed coverage gate (display-eligible rows must carry an ordinal); SNDK label-text fallback (`resolve_via_label_text`) recovers legacy prose rows to a face concept; `role_to_axis` made hyphen/underscore-insensitive (kebab-case face roles were dropping BS/CF arcs). Frontend `Viewer.tsx` adds an **As Reported / Standardized** toggle; `StatementMatrix` honors `display_negated` for true PDF sign; PDF number formatter. Two nullable migrations (`2026060500_add_display_label`, `2026060700_add_display_negated`) — already applied to production DB.
  - **Whole-row redundancy dedup** (`sec_json_adapter.dedup_redundant_rows`, wired into `attach_display_to_batch`): As-Reported showed duplicate rows when a legacy prose fact AND a capture-everything tag fact denote the same economic line (SNDK divestiture as 3 rows; LITE income_before_taxes). Design converged via **3-model review** (Opus 4.8 / Fable 5 / Codex GPT-5.5) which killed a per-cell v1 (frontend renders per-rowId → cells resurrect) → v2 **whole-row suppression**: null `display_label`+`ordinal` across ALL periods, ONLY when every displayed (non-YTD) period has a value-agreeing winner (fail-safe otherwise; frontend zero-change). Key A = prose long-tail vs same-**full-qname** tag long-tail; Key B = tag long-tail vs core uni in a separate `DEDUP_EQUIVALENT_CONCEPTS` registry (never touches `CANONICAL_CONCEPT` borrow path). Value-disagreement veto compares **magnitude** (prose can be pre-negated legacy data: raw −34 vs tag +34, same |34|) — value as VETO not match-key. Spec `docs/superpowers/specs/2026-06-10-dedup-display-suppression-design.md`.
  - **SNDK Q2 re-parse**: `build_separated --start-date` filters on `reportDate` (period_end); default `2025-01-01` cut off Q2_FY2025 (period_end `2024-12-27`), leaving the divestiture prose row without a tag competitor → widened to `2024-06-01` so the Q2 tag exists → prose row fully suppressible. cal sum sanity 45✅/0❌; IS periods unchanged.
  - **Review (T3 multi-AI adversarial)**: 2 rounds, Codex GPT-5.5 + Opus 4.8. Round-1 on the full merge diff: Codex caught a blocker Opus missed — Key A matched on bare-local names (namespace homonym `us-gaap:Foo` vs `ext:Foo` could wrongly suppress). Fixed (`_face_fullqname` recovers the tag's full qname from face edges; fail-closed on ambiguity; new homonym test). Round-2 re-review: both converged **BLOCKER CLOSED** (Opus proved no reachable false-negative — prose only resolves to on-face concepts). Production display byte-identical (SNDK/LITE single-namespace), only provenance winner string gained the namespace prefix.
  - **Production**: SNDK + LITE re-upserted `--apply` (auth'd); DB-verified loser cells have `display_label`/`ordinal`=NULL with value retained (suppress-not-delete); winner rows intact. MU/INTC/AAOI are a provable dedup no-op (zero prose facts). Frontend visually verified (As Reported): SNDK divestiture single row `(Gain) loss on business divestiture`, LITE income-before-taxes single row.
  - **Evidence**: 391 pytest (incl. 9 dedup, the homonym fail-closed test) + 50 vitest + tsc clean on merged main. Worktree `statement-view-pdf-faithful` merged + removed.
  - **Follow-ups (ADR-005, non-blocking)**: store full `source_account_qname` on tag long-tail rows (Codex suggestion, would remove the `_face_fullqname` recovery); parse-window data contract (SNDK reportDate / LITE arc-window); `is_long_tail` invisible in Standardized view; CF-instant veto + BS/CF dedup coverage (dedup currently only triggers on IS); optional re-upsert SNDK/LITE to refresh provenance winner string.
- **✅ P2.2 fy_end_month resolver fail-closed — DONE (CC `863e056`, no production write)** (2026-06-04). Hardened the 2026-06-03 off-by-one fix: `resolve_fy_end_month` now HARD FAILS (SystemExit) when neither `--fy-end-month` nor ticker_config `fiscal_year_end_month` resolves, instead of silently defaulting to 12 (which would still mislabel a non-Dec unconfigured filer like SNDK by one fiscal quarter). New `--allow-calendar-year-default` loud opt-in for true Dec filers. Both `parse_instance_xbrl.py` + `parse_def_xml.py`; `test_period_label.py` 11 tests (unknown-ticker now asserts SystemExit); synced 3 runtimes; SKILL.md CHANGELOG. Current 5 tickers all configured → no data impact; gate protects the next unconfigured-ticker run. **Both Codex P2s (P2.1 + P2.2) now closed.**
- **✅ P2.1 derive-base Q2/Q3 reconstruction — SHIPPED to production, Codex signed off, additivity proven** (2026-06-04). Closes the Codex P2.1 gap (YTD-CF tickers lost Q2/Q3 single-quarter IS/CF flows + quarterly TTM analytics after the YTD-first-class reparse). Followed review-before-prod: 2 Codex rounds (no P1; 1 P2 + 2 P3 all fixed) → user authorized apply. Migration `20260604120000` applied via Management API (constraint now allows derived_q2/q3); re-upserted all 5 tickers `--apply`, 0 rejected. **Additivity proven both ways**: pre-apply vs old DB = 0 changed / 0 removed across 2115 keys; post-apply DB == verified local exactly (0 diff, 5 tickers). DB derived_q2/q3: INTC 22/22, AAOI 36/37, SNDK 0/22 (spin-off, no Q1_FY2025), MU 95/81, LITE 98/102 (= derive-base single quarters + EL1 FCF/EBITDA at those quarters). Spot: INTC Q2_FY2025 OCF=2050 (rule Q2_6M_MINUS_Q1), net_debt_to_ebitda ttm @ Q4_FY2025/Q1_FY2026 (new TTM windows spanning derived Q2/Q3). Handoff `tmp/2026-06-04-derive-base-q2q3-review.md` (§9 round-1, §7 additivity proof). Commits: CC `b4bc7af`+`0edbb38`; AI `17209b2`+`5ad5e07`+`c4b5a5d`. **Frontend visual verify pending** (deferred to frontend Build). **Next: P2.2 (fy_end_month fail-closed) → frontend Build.**
  - **derive-base** (CC `b4bc7af`): new `rules_q2q3.py` (`q2q3_candidates`: Q2=6M−Q1, Q3=9M−6M, `period_kind=derived_q2/derived_q3`, `rule_id=Q2_6M_MINUS_Q1`/`Q3_9M_MINUS_6M`) mirroring `rules_q4` guards + reusing its predicates (rules_q4 byte-untouched). Engine Pass 2 runs q4+q2q3 (independent, both read `facts_after_p1`); Pass 3 identity filter widened to `{derived_q2,q3,q4}`. audit.py surfaces q2q3 skips. Telescoping exact (Q1+q2+q3+q4=FY).
  - **derive-analytics** (CC `0edbb38`, SCOPE EXPANSION found in dry-run): `_SINGLE_Q_KINDS += derived_q2/q3` so TTM windows (ROE/ROA/ROIC/efficiency/net_debt_to_ebitda) compute Q2/Q3-ending. One set covers `_index_flows` (IS) + `_index_cf_flows` (CF).
  - **migration + sync** (AI `17209b2`): `20260604120000_add_derived_q2_q3_metrics.sql` (DROP+ADD `sfm_period_kind_check` += derived_q2/q3, constraint-widening only, **NOT APPLIED**); `_shared/period_kind.py` VALID_KINDS; frontend `types.ts` + `useFinancialMatrix` QUARTERLY_PKINDS_IS_CF += derived_q2/q3; schema/data-rules/view-schema docs enums. tsc clean.
  - **drift reconciled**: derive-base prototype (`tmp/derive-base`, holds the tests) was BEHIND canonical on 3 files → forward-synced first so TDD ran on a faithful copy + final port byte-identical. **P3 (deferred)**: derive-base not yet on the ADR-001 canonical-SSOT-with-tests layout.
  - **Evidence**: derive-base 86 / derive-analytics 125 tests / tsc clean; all 5 dry-run gates 0 rejected/0 conflict/unique; INTC Q2_FY2025 OCF=2050=6M(2863)−Q1(813) same tag. **Anomalies resolved**: SNDK derived_q2=0 = correct (spin-off, no Q1_FY2025); LITE TTM unchanged = correct (LITE discloses direct single quarters → fix no-op; derived q2/q3 only for YTD-only items, D&A direct→skipped not duplicated).
  - **Remaining after review**: apply migration → re-upsert 5 (`--apply`, per-write auth) → empirical additivity spot-check → P2.2 (fy_end_month fail-closed) → frontend Build.
- **⚠ NEXT-SESSION FIRST (Codex review of 2026-06-04 session changes — 2 P2 deferred, before frontend Build)** (2026-06-04). Codex functional-reviewed this session's 5 production changes (handoff `tmp/2026-06-04-session-production-changes-review.md`): no P1; **2 P2 to fix first, next session**:
  1. **P2.1 derive-base Q2/Q3 gap (主要)**: the INTC/AAOI/SNDK re-parse removed YTD-disclosed Q2/Q3 single-quarter CF flows (old parser silently derived them; new parser correctly does not), but **derive-base only has Q4 rules** (`rules_q4.py`; `derive_engine` emits only `derived_q4`) → Q2/Q3 single-quarter CF flows + quarterly TTM analytics are now MISSING for YTD-CF tickers (INTC confirmed; check AAOI/SNDK/MU/LITE). Violates the parse contract "derive-base 靠 YTD 算 Q2/Q3/Q4" — only Q4 was built. **Fix**: add additive rules `Q2=6M−Q1`, `Q3=9M−6M` (IS/CF), `period_kind=derived_q2/derived_q3`, provenance same tier as Q4 (mirror rules_q4.py) → re-run derive chain + re-upsert affected tickers. TDD + own Codex round.
  2. **P2.2 fy_end_month fail-closed (小)**: `resolve_fy_end_month` must HARD FAIL when no ticker_config `fiscal_year_end_month` AND no `--fy-end-month` (not warn-then-default-12); add explicit `--allow-calendar-year-default` for a true calendar default. Current 5 tickers configured → this round safe; fix before next unconfigured-ticker production run.
  - **Then**: frontend Build (PDF-faithful Statement view) — plan `docs/superpowers/plans/2026-06-04-statement-view-pdf-faithful-display.md`, spec v5 closed; run in git worktree, subagent-driven.
  - Gap nature: missing-row (no wrong values), not corruption. Review NOT signed off until P2.1/P2.2 land.
- **MU supplement 全 23 期 + parse_instance_xbrl off-by-one 修復 + stale-parse 現代化（INTC/AAOI/SNDK re-parse）+ net_debt_to_ebitda rollout** (2026-06-03). 一條龍把美光收尾並把 derive-analytics 引擎 rollout 補齊到全 ticker：
  - **parse-SEC-supplement MU 全 23 期（FY2021Q1→FY2026Q2）NLM-first**：MU 首次用 XBRL-primary 單獨抽，NLM cross-check 抓到 `parse_instance_xbrl.py` **系統性 off-by-one 財季標籤** bug（Nov 27 2025 被標 Q4_FY2025 應為 Q1_FY2026，4/4 季一致）。改全期 NLM-first（缺期=primary、已抓期=validator，達 10K/10Q 同強度），footing 全綠（segment Σ=product Σ=consolidated，23 期 0 失敗）。XBRL off-by-one 檔 `QUARANTINED_offbyone_*`。Q4_FY2021–2024 單季 segment/product = 舊 8-K 未揭露（no-compute 留空，下游可 FY−9M）。建 supplement `MU.json` config（27 期）。Supabase `sec_financial_dimensional_facts` 374 rows。審計 `MU_supplement_VALIDATION.md`。
  - **工具 bug 根治**：`parse_instance_xbrl.py`/`parse_def_xml.py` 的 `--fy-end-month` 預設 12，非 12 月底 filer（MU=8、LITE=6）沒傳 flag 就靜默早標一季。`derive_period_label` 公式本來就對，缺的是 fy_end_month 來源 → 加 `resolve_fy_end_month(ticker, cli)`（CLI > ticker_config `fiscal_year_end_month` SSOT > 12 loud warning），default 12→None；全 config 補 `fiscal_year_end_month`（MU=8/LITE=6/INTC=12/AAOI=12）。TDD `test_period_label.py`（8 tests）+ 整合驗證。CC commit `08e690c` + 4 mirror sync。
  - **stale-parse 現代化（B：re-parse INTC/AAOI/SNDK）**：三者 production parse 早於 2026-05-17 YTD-first-class + CF D&A composite。逐 ticker re-parse → build_separated（cal sanity 全 0 ❌）→ diff-gate（**三者 CHANGED=0**，零值漂移）→ derive-base → derive-analytics → upsert。INTC：+YTD/CF D&A、−舊 derived CF Q2/Q3 單季（改 derive-base 重建），**EBITDA 解鎖**（FY2025 14354，D&A 11706=Depreciation+Amortization）。AAOI：+12 CF D&A、CHANGED 0。SNDK：本就現行（0 變動），re-upsert 補 net_debt。
  - **net_debt_to_ebitda rollout（A）**：引擎/前端早有（CC ec91e68），但只 MU 跑過。LITE re-upsert 補 24 rows；INTC/AAOI/SNDK 經 B 補上。**EBITDA 現在全 5 ticker 有值**：MU 17 / LITE 30 / INTC 4 / AAOI 16 / SNDK 3；net_debt_to_ebitda MU 5 / LITE 24 / INTC 1（AAOI/SNDK 0 = 虧損/期數少，設計如此）。
  - **測試**：`test_analytics_fallback_matches_skill_registry` 改為跳過 DEPRECATED.md 鏡像（ADR-001 退役 prototype 不該 gate 4 個 live mirror）；scripts **275 passed**。
  - **待**：(1) 前端視覺驗證新後端資料（INTC EBITDA + CF Q2/Q3 單季從 derive-base + net_debt_to_ebitda）= 用戶排序 ③；(4) supplement Stage-B-always 設計變更 + 其他 ticker supplement 重驗（spawned 工單）。
  - **✅ (2) AAOI derive-base NLM 20 ❌ 查證完成（2026-06-11）= 全部 NLM 端 artifacts，derive-base 正確、無需修**：3 類 —— (a) rounding ~14 筆：Q4=FY−9M 各自四捨五入累積 diff 1-2 千、tolerance 0.5 太緊（非錯）；(b) interest_expense 符號 2 筆：magnitude 一致（~1754）、derive-base 正費用符號 vs NLM 負，純符號慣例；(c) SG&A 3 筆：AAOI 拆 S&M+G&A 兩子科目報，derived 是合併（正確，如 Q4_FY2023 = FY 64295 − 9M 47501 = 16794），NLM 只讀了 S&M 子科目（3438 = 11069−7631，漏 G&A）。結論：NLM validation 的 tolerance/符號/子科目讀取不完整造成的 false-positive，derive-base Q4 重建正確。
  - **✅ (3) net_debt_to_ebitda 獨立 review 完成（2026-06-11）= 正確、無需修**：公式 net_debt@期末 / EBITDA_TTM(NI+NCI+interest+tax+D&A) ✓；net_debt = IC-debt(exact-value dedup，與 debt_to_equity/ROIC 一致) − cash − STI，cash 缺 skip、負淨債(淨現金)照 emit；EBITDA≤0 skip。真實值 INTC 0.90/LITE 0.20/MU -0.39(淨現金) 合理。
- **derive-analytics — 開發流程強化（ADR-001，四項）** (2026-06-02). 用 NLM「Topic - Project Dev」對照軟體工程最佳實踐後採用：(1) **先審再上 production**（Shift Left：TDD+property+dry-run 驗 → ADR handoff → Codex review → 收斂 → 才 `--apply`，改掉舊的「上線後才 review」）；(2) **canonical = SSOT**，退役 `~/AI_Agent/tmp/derive-analytics/` 扁平 prototype（git-ignored、手動 cp 易漂移），5 鏡像→4（canonical + 3 runtime），留 `DEPRECATED.md` 標記不刪；(3) **property-based 測試**對抗 confirmation bias/幻覺放大 — 新增 `tests/test_properties.py`（Hypothesis，11 tests）fuzz 引擎純守衛（EL2 `_kind_value`/`_nopat`/`_day_after`/`_yoy_candidate`、EL1 除零/`denom_skip`/dedup），人類仍最後把關；(4) **handoff 升級 ADR 格式**（Context/Decision/Alternatives/Consequences）。流程契約寫進 `docs/derive-analytics-workflow.md`。Property fuzzing 已暴露真實 robustness 缺口並修：`_day_after` 補 `OverflowError`（`date.max+1天`，原 except 未接 → 改成 graceful None；嚴格更安全、不改既有行為）。Tests：property 11 + crossperiod 44 + rules_ratios 48 + audit/scope 7，0 regression；4-mirror 同步驗證（3 runtime 皆有 test_properties + overflow guard）。指標數值/production 資料**未動**（純流程+測試+robustness）。
- **derive-analytics — Phase 4: EBITDA + EBITDA margin shipped to production** (2026-06-02). EL1 composite (same engine as FCF), NOT EL2. **Definition NLM-confirmed** (`Parse_SEC_Filings` notebook, SEC C&DI 103.01/103.02): EBITDA "Earnings" MUST be GAAP net income (bottom-up), NOT operating income — computing from operating income would be "Adjusted EBITDA" (operating income excludes non-operating items EBITDA adds back). So `ebitda = (net_income + net_income_nci[optional]) + interest_expense + income_tax_expense + depreciation_and_amortization` — base is CONSOLIDATED net income (net_income + NCI, reconstructing ProfitLoss; no-NCI issuers get +0) to stay consistent with the consolidated add-backs (Codex P2.1 convergence — chosen over net_income_total_pre_nci which the 3 no-NCI tickers don't tag); D&A from the CF non-cash add-back; marked `provenance.basis='GAAP_INPUTS_DERIVED_NON_GAAP_MEASURE'` (derived non-GAAP, not a GAAP line item — Codex P2.2); `ebitda_margin_pct = ebitda / revenue`. Two EL1 AnalyticsRules (cross IS+CF composite): ebitda (denominator-empty absolute value, output_statement=IS, output_unit=FROM_INPUTS USD) + ebitda_margin_pct. **All four add-backs required** → a period missing D&A is skipped rather than mislabelled. Engine CC `a914230`; frontend AI `71a76ad` (IS_ROWS += ebitda after Net Income; ebitda_margin_pct already in RATIO_ROWS; FALLBACK += 2 ids; schema registers both). Re-upserted 4 tickers: ebitda LITE 30 / AAOI 10 / SNDK 3 / **INTC 0** — INTC correct & expected: INTC splits D&A into separate Depreciation + AmortizationOfIntangibleAssets tags; the composite fallback is committed but INTC is not yet re-parsed (hits the deferred stale-parse YTD-flip), so production D&A=0 → EBITDA skipped; restored when INTC is re-parsed. LITE FY2021 EBITDA 706.9M / 40.6% margin (frontend preview verified: IS EBITDA row + Ratios EBITDA Margin %, negative in loss years, Adjusted EBITDA Margin empty = no 8-K Non-GAAP). Tests: prototype 99 / canonical 99 / scripts 275, 0 regression; tsc clean (Codex P2 closure round added +4: consolidated-NCI base, no-NCI-unchanged, non-GAAP basis marker, interest_and_other_net-only skip). Review handoff `tmp/derive-analytics-el2-phase4-ebitda-review.md` (Codex-closed; engine net_income+NCI solution accepted over its pre-NCI proposal). **Not done this round (non-blocking residuals)**: Net-Debt/EBITDA (EL2, needs TTM EBITDA — deferred); INTC EBITDA (needs re-parse for D&A); Adjusted EBITDA (Non-GAAP, 8-K pipeline); EBITDA UI tooltip for the non-GAAP basis (data contract correct, visual marker pending).
- **derive-analytics — EL2 expansion Phase 3: ROIC shipped to production** (2026-06-02). `roic` = TTM NOPAT ÷ avg invested capital (composite numerator + denominator, spec §3/§8.F). NOPAT = TTM operating_income × (1 − TTM income_tax_expense/TTM income_before_taxes); invested capital = total_equity + interest-bearing debt (short_term_borrowings + current_portion_of_long_term_debt + long_term_debt, exact-value deduped like debt_to_equity) − cash − short_term_investments. required: equity/cash/operating_income/tax+pretax; optional-as-0: STI + 3 debt terms. **Skip (never clamp)**: pretax≤0, tax_rate<0 or >1, avg invested capital≤0, missing TTM component/endpoint. quarterly ttm_duration + annual fy_annual_duration; period_start = day-after opening equity instant; only `roic` stored (NOPAT in provenance). Dedicated `_emit_roic` + `_invested_capital`/`_ttm_sum`/`_nopat` helpers (not the CrossPeriodRule shape). ROE/ROA + efficiency + YoY unchanged. Engine CC `718be37`; frontend AI `460a2be` (RATIO_ROWS += roic, TTM_RATIO_ROWS += roic, FALLBACK += RATIO_ROIC, schema §RATIO). Re-upserted 4 tickers: roic LITE 12 (10 ttm + 2 annual, FY2021 27.5% / FY2022 18.6%); AAOI/INTC/SNDK 0 — **verified correct**: AAOI income_before_taxes is negative every quarter (chronic losses → TTM pretax≤0 → skip; roe emits with negative values but roic skips losses by design, ROIC only meaningful for profitable operations); INTC same + data window; SNDK too few periods. ROE/ROA unchanged (LITE 28/28); roic provenance carries cell_ids, unit Pure. Frontend preview verified (LITE annual ROIC 27.5%/18.6%, loss years "—", ROIC>ROE reflecting low leverage). Tests: prototype 43 (+8) / canonical 90 / scripts 275, 0 regression; tsc clean. Review handoff `tmp/derive-analytics-el2-phase3-roic-review.md`. **EL2 expansion Phases 1–3 (efficiency + YoY + ROIC) all shipped.** Remaining: Phase 4 EBITDA (blocked on re-parse INTC/AAOI/SNDK for D&A + NLM definition).
- **derive-analytics — EL2 expansion Phase 2: YoY growth (revenue/net_income/eps_diluted) shipped to production** (2026-06-02). New cross-period path (distinct from the TTM/avg rules): `(current − year_ago) / year_ago`, prior>0 required (growth off a non-positive base undefined → skip). quarterly = current single-quarter vs year-ago same quarter, period_kind **inherits the current fact's kind** (quarter_duration / derived_q4 for Q4 — both route via the frontend quarterly fallthrough; YoY is NEVER ttm_duration); annual = fy_annual_duration. period_start=None (FactRow has no duration startDate). inputs reference current + year-ago facts by cell_id. EPS has no derived_q4 (not additive) → eps_diluted_yoy quarterly is Q1–Q3 only. Engine CC `ff6b37e` (`YOY_RULES` + `_emit_yoy`, called at the end of `compute_crossperiod_metrics`; `ALL_CROSSPERIOD_RULE_IDS` unions 3 YoY ids; ROE/ROA + efficiency unchanged). Frontend AI `e7f9f7e`: RATIO_ROWS += 3 YoY rows (pct-style default, NOT in TTM_RATIO_ROWS); upsert FALLBACK += 3 ids; schema §RATIO registers 3 uni_accounts. Re-upserted 4 tickers: LITE rev_yoy 28 / ni_yoy 14 / eps_yoy 12; AAOI rev_yoy 11 / ni_yoy 0 / eps_yoy 0 (loss-year prior≤0 skip, expected); INTC rev_yoy 1; SNDK 0. LITE revenue_yoy FY2025 = 21.0%; 0 YoY rows are ttm_duration. Frontend preview verified (annual Revenue YoY 3.8%/(1.7%)/3.2%/(23.1%)/21.0%; loss-year YoY shows "—"). Tests: prototype 34 (+8) / canonical 81 / scripts 275, 0 regression; tsc clean. Review handoff `tmp/derive-analytics-el2-phase2-yoy-review.md`. Next: Phase 3 ROIC (§8.F) → Phase 4 EBITDA (blocked on re-parse + NLM).
- **derive-analytics — EL2 expansion Phase 1: efficiency metrics (asset_turnover + DSO/DIO/DPO/CCC) shipped to production** (2026-06-02). Design spec `docs/superpowers/specs/2026-06-02-derive-analytics-el2-expansion-design.md` (covers Phase 1 efficiency + Phase 2 YoY + Phase 3 ROIC) Codex-reviewed: all 6 decisions approved, 5 P2 + 1 P3 contract gaps folded into §8 (per-phase period_kind, days as Pure display category not storage unit, YoY period_start=None + prior>0, extend TTM_RATIO_ROWS, ROIC required/optional + abnormal-tax skip). **Decision-independent engine pre-work** (CC `6276fdd`): generalized EL2 flow indexing (`_index_flows`/`_index_balances`) so any IS flow (revenue/cost_of_goods_sold/net_income) can be a TTM/FY numerator — ROE/ROA byte-identical vs production DB (LITE 56 cells, 0 changed). **Phase 1 engine** (CC `0e59a82`): rule-outer loop on the generic index; `CrossPeriodRule` gains `numerator_uni` + `kind` (`ratio` = TTM flow/avg balance; `days` = 365×avg balance/TTM flow). New rules: `asset_turnover` (revenue/total_assets, x), `dio/dso/dpo` (cogs|revenue / inv|AR|AP, days), `ccc` = dio+dso−dpo (second pass, all three required else skip). Skip policy: ratio avg≤0; days ttm≤0 or avg≤0; CCC missing-component. payables uses COGS (purchases proxy); days basis fixed 365 (52/53-week ≤1.6% known limit). ROE/ROA preserved byte-identical (loop restructure only reorders emission — verified vs DB). **Frontend** (AI `29b61bd`): new `days` display category (`RATIO_AS_DAYS`, `fmtValue` → "49.3 days", `chartGroupOf` own axis, `UnitGroup += days`); `asset_turnover` → `RATIO_AS_MULTIPLE` (x); `TTM_RATIO_ROWS` extended to the 5 keys; upsert FALLBACK += 5 rule_ids; schema §RATIO registers 5 core uni_accounts. **Re-upserted 4 tickers**: efficiency rows AAOI 11 / INTC 1 / LITE 28 / SNDK 0 (matches roe/roa data-window coverage); CCC self-consistent (LITE FY2025 143.71+49.34−58.16=134.89); all unit=Pure. Frontend preview verified (LITE quarterly: Asset Turnover 0.40x, DSO 49.3 days, DIO 143.7, DPO 58.2, CCC 134.9). Tests: prototype 25 (+7) / canonical 72 / scripts 275, 0 regression; tsc clean. Review handoff `tmp/derive-analytics-el2-phase1-efficiency-review.md`. Next: Phase 2 YoY → Phase 3 ROIC → Phase 4 EBITDA (blocked on re-parse INTC/AAOI/SNDK + NLM definition).
- **parse-10QK-gaap — LITE accounts_receivable ASC 606 fix + quick_ratio coverage + D&A rollout (option A), shipped to production** (2026-06-02). Diagnosed why LITE `quick_ratio` only covered 3/27 periods: LITE adopted ASC 606 in FY2018 and reports its balance-sheet "Accounts receivable, net" line under `us-gaap:ContractWithCustomerAssetNet` (the standard `AccountsReceivableNetCurrent` tag stops at 2018-06-30; the contract-asset tag runs 2018-06-30→2026-03-28, label = "Accounts receivable, net"). `BS_TAG_MAP["accounts_receivable"]` only knew the standard tag → LITE AR vanished from 2018+ → quick_ratio skipped (AR is a required term). Secondary bug: label-discovery mis-mapped `NontradeReceivables` (us-gaap label "Nontrade Receivables" contains the substring "trade receivable") to accounts_receivable (3 periods). **The contract-asset→AR mapping is a ticker-specific override, NOT a global fallback** (Codex review convergence — `ContractWithCustomerAssetNet` is semantically an ASC 606 contract asset, so a blanket fallback would mis-count it as AR for a future issuer reporting a genuine contract asset; xbrl_extract only has the standard us-gaap label which lacks "accounts receivable", so the ticker is the available signal). Implementation: `TICKER_BS_TAG_OVERRIDES = {"LITE": {"accounts_receivable": ["ContractWithCustomerAssetNet"]}}` + `bs_tag_map_for_ticker(ticker)` applied at the `discover_tags_for_company` call site (override appended after the standard tag → first-match-wins keeps standard primary; global `BS_TAG_MAP` stays clean); `METRIC_LABEL_RULES["accounts_receivable"]` negative keywords += `"nontrade"` (guards the "Nontrade Receivables" substring false-positive). **Cross-ticker safe**: AAOI/INTC/SNDK use the standard tag and none report `ContractWithCustomerAssetNet`; the override only fires for LITE. Verified (re-parse LITE): AR 3→27 periods, all sourced from `ContractWithCustomerAssetNet`, Q4_FY2025 = 250.0M exact vs companyfacts; **BS current-assets reconcile to diff=0.0** (cash+STI+AR+inventory+other = AssetsCurrent, 4 periods — stronger than NLM, the AR value fills exactly the gap); 24 audited cells preserved; quick_ratio 3→27 periods with invariant cash≤quick≤current holding; frontend annual Quick Ratio renders all columns (FY2020 6.32x…FY2025 2.87x). **Ticker-override re-parse is byte-identical to the prior global-fallback parse (0 added/removed/changed) → production needs no re-upsert.** `test_ar_tag_map.py` (7 passed) + SKILL.md CHANGELOG/Known Limitation + authority-schema notes (`sec-financials-v2-schema.md` §2.1 + `financials-view-schema.md`). Deployed CC `1146da1` (initial fix) → `5464722` (ticker-override refactor) + 3 runtimes; AI `8e7a92a` (STATUS) + `dfc67f2` (schema docs). **Bundled D&A composite rollout (user-approved option A)**: re-parsing LITE with current parser code also pulled in the previously-committed-but-deferred CF D&A composite fallback (couldn't isolate AR — the parser produces both). LITE re-parse was clean: **0 existing-value changes** (none of the YTD-flip that blocked the INTC rollout — LITE is not stale), purely additive — D&A core `depreciation_and_amortization` 1→41 periods (local `gaap_facts.json`), derive-base Q4 D&A reconstruction +6 (Q4_FY2020–2025), **unblocking EBITDA for LITE**. Full LITE pipeline re-run (parse → derive-base → derive-analytics → upsert --apply): facts local `gaap_facts.json` 2286→2432 / DB `sec_financial_facts` 2578→2724 (+146 each), edges local cal 2932 + pre 5835 = 8767 / DB `sec_financial_edges` 8794 (`--limit 32` captured more filing linkbases — relationship metadata only, gate-validated unique, not value drift), metrics analytics 360. Review handoff: `tmp/parse-ar-contract-asset-review.md` (§9–§11 = ticker-override convergence). Known leftover: 1 cal-sum-sanity ❌ (Q4_FY2022 IS GrossProfit) — pre-existing derived-Q4 / linkbase-role artifact (current facts have no IS Q4_FY2022 gross/rev/COGS rows; bad row only in the cal-sanity check), no causal link to the BS/CF changes (0 existing-value diffs confirm); Codex agreed it is not a storage blocker. **Codex review converged (LITE production correct; parser contract narrowed to ticker-override).** Next: EBITDA can restart on LITE (other tickers still need re-parse); quick_ratio now correct for all 4 tickers.
- **derive-analytics — EL2 cross-period engine: ROE / ROA shipped + deployed** (2026-06-02). First trailing-12-month (TTM) metrics. ROE = TTM net_income / avg total_equity; ROA = / avg total_assets. Quarterly rows use a NEW `period_kind='ttm_duration'` + `provenance.window='TTM'` (period = TTM end quarter); annual rows use `fy_annual_duration`. Design spec (`docs/superpowers/specs/2026-06-01-derive-analytics-el2-cross-period-design.md`) converged over 3 Codex rounds. **DB migration applied to production** (`supabase/migrations/20260602000000_add_ttm_duration_metrics.sql`: DROP+ADD `sfm_period_kind_check` to allow `ttm_duration`, metrics-table only) via the Supabase Management API; 5-place enum/docs sync (schema §0.3, data-rules display table, view-schema, `types.ts` PeriodKind, `_shared/period_kind.py`). New engine modules `period_topology.py` (parse_period / trailing_quarters with fiscal-year wrap / year_ago_quarter / prior_fy) + `rules_crossperiod.py`, parallel to EL1 `rules_ratios`. TTM net_income sums the trailing 4 single-quarters (`quarter_duration ∪ derived_q4`, excludes `ytd_duration`); 2-point average balance ((begin+end)/2; quarterly begin = year-ago same quarter, annual begin = `Q4_FY{y-1}`); skips on avg≤0 / missing quarter / missing endpoint / earliest-FY-no-prior. `RatioCandidate` gained a `window` field; the audit writer emits `provenance.window`; the runner appends EL2 after EL1; `build_managed_rule_ids` and the upsert fallback union EL1∪EL2 (RATIO_ROE/RATIO_ROA); drift guard now checks both modules across 5 mirrors. Frontend: `roe`/`roa` already in RATIO_ROWS; `useFinancialMatrix` RATIO quarterly view routes `ttm_duration` cells (annual stays `fy_annual_duration`). Deployed CC `7c10ca0` + 3 runtimes; AI `8ab16d0` (migration+enums) + `40bf9e0` (wire). Re-upserted 4 tickers: ROE/ROA rows AAOI 11 (9 ttm+2 annual) / INTC 1 / LITE 28 (23+5) / SNDK 0 (INTC/SNDK low because few periods — TTM needs trailing-4 + year-ago balance; expected). `ttm_duration` appears only on roe/roa. Spot-check: LITE FY2021 ROE = 397.3 / avg(1749.2,1972.8) = 0.2135 (exact). Lineage audit (Codex gate 5): TTM net_income inputs are quarter_duration/derived_q4, no ytd. **Codex functional review converged over 4 rounds (no remaining P1/P2)**. **Convergence round (period_start + frontend allowlist), shipped to production**: (1) **EL2 `period_start` now derived** — both `ttm_duration` and annual EL2 rows persist `period_start = opening-balance instant + 1 day` (`begin_f.period_end + 1` via `_day_after()`; `begin_label = year_ago_quarter` is by construction the quarter just before the window's first quarter, and fiscal periods tile gaplessly, so this is the exact window first-day == eventual XBRL context startDate — a real sourced date, no parse-layer change, no fabrication; None-safe on unparseable dates). Codex's literal "from `ni_facts[0].period_start`" was infeasible (FactRow carries no start date) — converged via mutual challenge to the opening-balance anchor instead. **Deliberate mixed contract**: EL2 rows carry period_start; EL1 single-period rows stay null (their window is label-derivable), to be unified in a future table-wide pass. Re-upserted 4 tickers `--apply`: all EL2 rows non-null period_start (LITE 56 / AAOI 22 / INTC 2 / SNDK 0), EL1 280 untouched (null), ROE/ROA values unchanged. Self-validating: LITE annual period_start tiles contiguously (FY2021 end 2021-07-03 → FY2022 start 2021-07-04 → …). (2) **Frontend `ttm_duration` allowlist** — `useFinancialMatrix` quarterly branch now requires `TTM_RATIO_ROWS = {roe, roa}` membership (was accepting any RATIO key; clean data today, guardrail vs future stray rows). **Frontend visual verify DONE** (LITE preview: quarterly Ratios shows TTM ROE/ROA, annual shows FY; TTM-ending-Q4 == annual; three-way hand-calc/DB/UI agreement). Deployed CC `43f1f0f` (period_start) + 3 runtimes; AI `8f5b70a` (frontend allowlist). Tests: prototype 63 (+3 period_start derivation/None-fallback) / scripts 275, 0 regression; tsc clean (the two suites must run separately — `test_crossperiod.py`'s `sys.path.insert` shadows same-named modules and pollutes `test_phase3_adapter_derive` in a shared pytest session; pre-existing harness fragility). Review handoff: `tmp/derive-analytics-el2-roe-roa-review.md`. **Future P3 (deferred, non-blocking)**: a window-length sanity gate to catch stub / fiscal-year-change periods where label topology (not the +1-day rule) could mis-anchor the window. Later EL2: turnover/DSO/CCC/YoY (same primitives), Net-Debt/EBITDA (blocked on EBITDA / stale-parse), ROIC.
- **parse-10QK-gaap — CF D&A composite fallback (EBITDA prerequisite)** (2026-06-01). Investigating EBITDA (NLM `Parse_SEC_Filings` confirmed standard formula = net_income + interest + taxes + D&A from CF, bottom-up; non-GAAP) surfaced a parse gap: INTC/LITE report depreciation + amortization as SEPARATE cash-flow add-backs (`us-gaap:Depreciation` + `us-gaap:AmortizationOfIntangibleAssets`) rather than the combined `DepreciationAndAmortization` tag, which `CF_TAG_MAP` only matched — so D&A was dropped entirely (INTC 0 periods, LITE 1; Intel's ~$10B/yr depreciation silently missing, also a CF-display hole). Fix (`xbrl_extract.py`, mirrors the SG&A composite fallback): when the combined D&A tag is absent for a period, sum a `Depreciation` slot + a single amortization slot (priority candidate `[AmortizationOfIntangibleAssets, AmortizationOfAcquiredIntangibleAssets]`, pick one — NOT summing variants) into `depreciation_and_amortization`; preserve components as `operating_cf_long_tail` (rolls_up_to=depreciation_and_amortization). The `long_tail_extras` emission is now section-aware (IS→is_rows, CF→cf_rows; was hardcoded to is_rows). Two Codex review fixes landed (both verified): **P1** — the guard was metric-level so a single stray value blocked the whole fallback; LITE had one mis-mapped `DisposalGroupIncludingDiscontinuedOperationDepreciationAndAmortization` value (Q2_FY2025=0.3) that skipped LITE's entire D&A fallback. Fixed two ways: (a) added "disposal","discontinued" to the D&A label-match negative keywords (root cause — that discontinued-ops tag's label contains "depreciation and amortization"); (b) made the fallback **period-level** (fill only periods the combined tag didn't cover, never overwrite). **P2** — preserve the actual amortization source tag per period in long-tail + log a collision warning when both intangible-amortization variants are present with different values. Confirmed via INTC calculation linkbase. **Verified in a temp vault** (no production touch): INTC FY2024 D&A=11379M (9951+1428), FY2025=11706M, no regression; LITE D&A now 15 periods (was just the stray 0.3), disposal-group gone, FY2025=254.0M = Depreciation 104.3 + Amortization 149.7 (exact companyfacts), Q2_FY2025 corrected to 64.9M; 0 leak into IS. Deployed CC `4ffa4d8` + `076ed72` + 3 runtimes; SKILL.md CHANGELOG + Known Limitation updated. Codex signed off across 3 passes (§8 P1/P2 → §9 fixes → §10 converge; P3 also fixed in `d36f145`). **Production rollout was attempted then ROLLED BACK** (DB untouched — no re-upsert): the old-vs-new diff gate caught that re-parsing INTC produces a **non-D&A change** — its production parse predates the 2026-05-17 "YTD 升 first-class" skill change, so a current-skill re-parse flips CF `Q2/Q3` single-quarter (silently reconstructed by the old skill) to `6M/9M` YTD first-class. The D&A fix computes YTD-period D&A, which can't be cleanly injected into the stale single-quarter production model. **Decision (user, option B): defer EBITDA**; the D&A skill fix stays committed/ready for when the tickers are re-parsed to the current contract. INTC restored from backup (`/tmp/parse_backup_20260601`). Review handoff: `tmp/parse-cf-da-composite-review.md` (§8 Codex P1/P2, §9 Claude fixes, §10 converge).
- **FUTURE PROJECT — re-parse all SEC tickers to current contract** (flagged 2026-06-01). INTC/LITE/AAOI/SNDK production parses predate the 2026-05-17 "YTD first-class + remove silent derive" change (and now the CF D&A composite fallback), so their `sec_financial_facts` are stale vs the current `parse-10QK-gaap` contract. A proper modernization re-parses each ticker to the current skill, then re-validates the downstream chain (frontend CF single-quarter↔YTD display, derive-base Q4/YTD reconstruction, derive-analytics ratios that read CF such as FCF/FCF-margin). Should be a staged, per-ticker, diff-gated effort — NOT bundled into a single metric add. EBITDA (+ EBITDA Margin, Net-Debt/EBITDA) depends on this landing first (needs CF D&A).
- **derive-analytics — Quick Ratio shipped + deployed** (2026-06-01). `quick_ratio = (cash_and_cash_equivalents + short_term_investments[optional] + accounts_receivable) / total_current_liabilities`, rule_id `RATIO_QUICK_RATIO`, BS-derived (instant), multiple-style (x display). **Naming decision (user-approved Option A)**: the plan's §206 concern ("don't call it quick_ratio without ST investments") was resolved by verifying AAOI/SNDK genuinely report NO marketable securities (absent even from BS long-tail; parse reads all 4 linkbases) — so `short_term_investments` is an EL1 optional term that is correctly 0 for them (giving (cash+AR)/CL, a real quick ratio, not degraded) and included for LITE/INTC. cash + AR are required (quick ratio is ill-defined without receivables → skip). Engine: 1 EL1 composite, `ALL_RULE_IDS` 10→11; FALLBACK += RATIO_QUICK_RATIO; frontend RATIO_ROWS + RATIO_AS_MULTIPLE += quick_ratio; schema §4. Deployed CC `86348f0` + 3 runtimes; AI `45758a3`. Re-upserted 4 tickers (AAOI 152 / INTC 60 / LITE 280 / SNDK 46). Verified invariant cash_ratio ≤ quick_ratio ≤ current_ratio across all sampled periods; provenance.inputs correctly omit short_term_investments for AAOI, include it for LITE/INTC. Known limitation: LITE emits only 3 quick_ratio rows because its `accounts_receivable` tag is parsed for only 3 periods (AR required → skip when absent; a parse-side AR-tag-coverage issue, not a rule bug). Tests: prototype 47 (+4) / scripts 275, 0 regression; tsc clean. Review handoff: `tmp/derive-analytics-quick-ratio-review.md`. Remaining (ascending risk): Phase C EBITDA (needs EBITDA-definition decision) → Phase D EL2 cross-period engine (needs TTM/avg-balance storage-contract decision) → Phase E segment margin → BVPS.
- **derive-analytics — P3 cleanup shipped + deployed** (2026-06-01). Cleared the two non-blocking P3s accumulated from the FCF / FCF-Margin review rounds: (1) `provenance.inputs` entries now carry the source fact's `unit` so the JSON artifact can independently audit FROM_INPUTS / cross-statement unit propagation (no need to re-query the source cell); (2) retired the one-cycle `ratio_metrics` payload mirror — `write_analytics_json` emits only the canonical `analytics_metrics` key, while the upsert reader keeps a defensive `ratio_metrics` fallback for any pre-existing artifact; runner wording "ratio rows"→"analytics rows". Deployed CC `cc7178e` + 3 runtimes; AI `abbc840`. Re-upserted 4 tickers (row counts unchanged 139/56/277/42 — pure provenance refresh); verified DB provenance.inputs carry unit and artifacts have only `analytics_metrics`. Tests: prototype 43 (+1) / scripts 275 (reader fallback test stays green), 0 regression; tsc clean. Review handoff: `tmp/derive-analytics-p3-cleanup-review.md`. Remaining derive-analytics roadmap (ordered by ascending risk): Quick Ratio (needs naming decision — `short_term_investments` absent for AAOI/SNDK) → Phase C EBITDA → Phase D EL2 cross-period engine (needs TTM/avg-balance storage-contract decision) → Phase E segment margin → BVPS.
- **derive-analytics — FCF Margin shipped + deployed** (2026-06-01). Follows FCF: `fcf_margin_pct = (net_cash_from_operating − capital_expenditures) / revenue`, rule_id `RATIO_FCF_MARGIN_PCT`, statement=RATIO, unit=Pure, **pct-style (can be negative)**. First **cross-statement composite ratio**: numerator from CF (CFO −capex), denominator from IS (revenue) — computed directly, NOT via a materialized-FCF dependency (algebraically identical to FCF/revenue but a single-pass EL1 rule with no rule-ordering risk). Verified CF and IS facts always share period_kind/period_end per period (both duration; YTD both skipped), so the consistency guard passes naturally. GAAP-only (no NON_GAAP CF facts to resolve). Engine: 1 EL1 composite rule, `ALL_RULE_IDS` 9→10. `DERIVE_ANALYTICS_RULE_IDS_FALLBACK += RATIO_FCF_MARGIN_PCT`; frontend `RATIO_ROWS += fcf_margin_pct` (after net_margin, pct-style not multiple); schema §4 registered. Deployed: CC `8a01ebb` + 3 runtimes; AI `6d1cd1e`. Re-upserted 4 tickers (AAOI 139 / INTC 56 / LITE 277 / SNDK 42 analytics rows). Spot-check: INTC Q1_FY2025 −0.3450 (FCF −4370 / rev 12667) ✓. Frontend (Claude Preview): FCF Margin % renders in Ratios tab, negatives in parens (34.5%). Tests: prototype 42 (+4) / scripts 275, 0 regression; tsc clean. Review handoff: `tmp/derive-analytics-fcf-margin-review.md`. **Codex signed off (§7): no P1/P2** — both §6 decisions accepted (direct computation over materialized-FCF dependency; pct-style classification). Two P3 caveats recorded in schema §4: (a) the formula equals only THIS pipeline's FCF (CFO−capex); a future management-disclosed FCF must NOT be mixed into `fcf_margin_pct` — set up a separate source-specific contract; (b) FCF rows without a same-period revenue are correctly skipped (LITE 19 FCF → 17 fcf_margin; Q4_FY2020/FY2021 lack reconstructable Q4 revenue).
- **derive-analytics — Phase B body (FCF) shipped + deployed** (2026-06-01). First **non-RATIO absolute-value derivation**: `free_cash_flow = net_cash_from_operating − capital_expenditures`, `statement=CF`, `unit=USD` (per-ticker scale), `rule_id=FCF_CFO_MINUS_CAPEX`, written to `sec_financial_metrics` (never pollutes facts). Brainstormed (approach A) + design spec `docs/superpowers/specs/2026-06-01-derive-analytics-phase-b-fcf-design.md`. Scope deliberately FCF-only (Quick Ratio deferred — `short_term_investments` absent for AAOI/SNDK, naming undecided). Engine (`rules_ratios`): `RatioRule`→`AnalyticsRule` (alias) gains optional `denominator` + `output_statement`/`output_unit`; a numerator-only rule emits an absolute value (no division), unit propagated from the resolved inputs (`FROM_INPUTS`; mismatched scale → skip), reusing the existing period-kind/period-end consistency + YTD-skip + provenance machinery — the 6 existing ratios are byte-for-byte unchanged. SEC capex is stored positive (cash outflow) so FCF subtracts (NOT the TWSE additive pattern); negative FCF emits. Payload key generalized `ratio_metrics`→`analytics_metrics` (one-cycle back-compat mirror); upsert reader accepts both, `DERIVE_ANALYTICS_RULE_IDS_FALLBACK += FCF_CFO_MINUS_CAPEX`. Frontend: `CF_ROWS += free_cash_flow` (renders as derived italic-muted cell). The Phase B-prereq facts-wins (all-statement) now actually engages for CF. Deployed: CC `a500660` (engine+payload) + 3 runtimes via `sync-to-local.sh`; AI `df402c9` (upsert+frontend+docs) + spec. **Re-upserted all 4 tickers** (AAOI 10 / INTC 6 / LITE 19 / SNDK 3 FCF rows; statement=CF, correct per-ticker units). Spot-checked vs SEC: INTC FY2025 −4949 (9697−14646), Q1_FY2025 −4370, AAOI Q1_FY2023 −9965 — all match. facts-wins 0 drops (no disclosed FCF facts). Frontend (Claude Preview): Free Cash Flow row renders italic-muted derived, negatives in parens. Tests: prototype 38 (+7 FCF) / scripts 275 (+3), 0 regression; tsc clean. Review handoff: `tmp/derive-analytics-phaseB-fcf-review.md`. **Codex functional review signed off (§9): no P1/P2 findings**; both §8 decisions accepted (FROM_INPUTS unit policy OK; do NOT storage-round the float artifact — display/chart rounding is correct, a storage round would muddy the value==input-arithmetic audit semantics). Two P3 deferred (non-blocking): (a) `provenance.inputs` could carry input `unit` for standalone JSON audit of FROM_INPUTS propagation (recoverable today via cell_id, so ergonomics only); (b) a few stale "ratio rows" comments/prints — clean up alongside removing the `ratio_metrics` back-compat mirror. Known minor: LITE FY2020 FCF stores float artifact `438.29999999999995` (display rounds; ratios have same). Still pending: Quick Ratio (naming), FCF Margin, C (EBITDA), D (EL2 cross-period), E (segment margin), BVPS.
- **derive-analytics — Phase B prerequisite shipped: storage-boundary facts-wins broadened to all statements + paginated** (2026-06-01). Before Phase B (Quick Ratio + FCF) and Phase C (EBITDA) can emit non-RATIO derived rows (FCF→`statement=CF`, EBITDA→`statement=IS`, both USD absolute values), the storage-boundary facts-wins guard had to stop being RATIO-only. The Phase 0 guard SELECTed only `statement='RATIO'` facts with a single un-paginated `execute()`; that was safe while derive-analytics emitted RATIO ratios only, but (a) it would never catch an FCF/EBITDA derived row colliding with a disclosed CF/IS fact, and (b) IS+BS+CF facts per ticker exceed Supabase's 1000-row default cap (live check: **AAOI 1108, LITE 2578** total fact-keys — the old fetch was silently truncating). `scripts/upsert_sec_financials.py` now has `fetch_facts_logical_keys(client, ticker, page=1000)` — drops the statement filter, pages via `.range()` (same pattern as `migrate_db_audit_source_v4.py`) — and `filter_ratio_rows_against_facts` was generalized to `filter_derived_rows_against_facts` (logic was always statement-agnostic; back-compat alias kept). The read-model API (`/api/financials/[ticker]`) was already broad + paginated, so only the storage boundary needed this. **No production data changed** — today's derived rows are all `statement=RATIO`, which can't collide with the newly-included IS/BS/CF keys; this is a pure forward guardrail. Two Codex P2 review fixes landed (both scrutinized + accepted): (1) the paginated fetch now carries a stable `.order("cell_id")` — without it Postgres/PostgREST guarantee no row order across separate `range()` requests, so adjacent pages could overlap/skip and silently drop fact-keys (the read-model API's three `fetchAllPages` selects got the same `.order("cell_id")` fix, since LITE already crosses pages there today); (2) the analytics apply path now does the read-only fetch+filter BEFORE the scope delete, so a mid-pagination failure can't leave analytics rows deleted-but-not-reinserted. Tests: scripts 272 (269 + 3: non-RATIO collision drop, pagination/all-statement+order fetch, fetch-fail-no-delete), 0 regression; `tsc` clean. Live read-only smoke confirmed pagination+order engages on AAOI(1108)/LITE(2578) with identical counts and zero errors. Review handoff: `tmp/derive-analytics-phaseB-prereq-facts-wins-review.md` (§7 Codex review, §8 Claude response + fixes).
- **derive-analytics expansion — Phase 0 (MVP contract hardening) shipped + deployed** (2026-05-31). Roadmap in `docs/derive-analytics-expansion-plan.md` (Codex-reviewed ×2). Before adding any new analyst metrics, the existing 5-ratio MVP was hardened to the same metrics contract as derive-base:
  - **0.1 Row schema**: `audit.py` now emits canonical `metrics_cell_id` (was a human-readable `derived::…` string), and `provenance` carries `formula` + `target_table`. `rules_ratios.py` `RatioCandidate` carries `formula` ("{num} / {den}") and each input records the source fact's `cell_id` (lineage; `facts_cell_id` for raw facts, `metrics_cell_id` for derived inputs — supplied free by the shared adapter).
  - **0.2 Owned-scope delete**: `derive_analytics.build_managed_rule_ids()` returns the full `ALL_RULE_IDS` registry (not reverse-derived from emitted rows); `upsert_sec_financials.py` adds `DERIVE_ANALYTICS_RULE_IDS_FALLBACK` + `analytics_delete_scope()` (union payload+fallback) so a rule that emits 0 rows this run no longer leaves stale Supabase rows.
  - **0.3 YTD skip**: ratios are no longer computed on `ytd_duration` (6M/9M) periods — those were orphan rows the frontend never renders. Production row counts dropped accordingly (LITE 213→159, AAOI 101→77, INTC 29, SNDK 32→24).
  - **0.4 facts-wins guardrail (storage + read-model)**: `/api/financials/[ticker]` drops any derived (metrics) row whose logical identity (`period|period_kind|version|statement|uni_account`) collides with a direct-disclosed (facts) row; and the upsert now enforces the same at the **storage boundary** (`filter_ratio_rows_against_facts()` — SELECTs disclosed RATIO facts and skips colliding derived rows before the metrics upsert, not just in the API). Zero overlap today (disclosed RATIO are NON_GAAP, derived are GAAP); forward guardrail.
  - **0.5 Freshness gate**: `verify_derived_freshness()` gained a `required_keys` param. The analytics `--apply` gate fails closed before `apply(batch)` on every unsafe state — incomplete run (exit 2), missing run when Supabase already has analytics rows (exit 4 unless `--allow-missing-derived`), and stale inputs (exit 3). `analytics_required_keys(derive_status)` additionally requires `derive_base` lineage whenever derive-base is loaded, and `analytics_derive_base_is_current()` further refuses when the analytics run points to an older derive-base run than the current latest (path-version drift the hash check alone misses) — so an analytics run can't ship raw-only or stale ratios beside fresh derive-base metrics. `client` is lazily obtained in the apply block (fixes an UnboundLocalError on the parse-only + analytics-loaded path).
  - Deployed: prototype synced to CC_Switch_Config (`974af9a` + `85df8f8`) + 3 runtimes; backend in AI_Agent (`bf6d246` + Codex-review fixes `8bec484` / `22d4a82` / `9d2388a`). All 4 tickers (LITE/AAOI/INTC/SNDK) re-run + re-upserted to production; verified 0 YTD orphans / 0 legacy cell_id / 0 missing formula / derive_base lineage present. Tests: scripts 269 + prototype 21, 0 regression. Three Codex functional-review rounds closed (5 + 1 + 1 findings).
  - **FCF/EBITDA boundary correction** (per Codex P1.5): FCF/EBITDA materialization will live in derive-analytics, NOT derive-base (derive-base stays a gap-filler). Phase A shipped: **`cash_ratio`** (CC `25a4729` / AI `5e4a866`), then **EL1 composite-term engine + `debt_to_equity` + `interest_coverage`** (CC `22099e6` / AI `5f732e8`). EL1 upgraded `rules_ratios` from one-account/one-account tuples to linear `Term(uni, statement, coef, required)` lists for numerator and denominator (formula generated from terms; single +1 term reproduces prior behaviour, so the 6 existing ratios are unchanged). debt_to_equity = interest-bearing debt (ST borrowings + current portion LTD + LTD, optional terms) / total_equity; interest_coverage = (IBT + interest_expense) / interest_expense (standard TIE, with a documented cash-rich-overstatement caveat). All 4 tickers re-upserted, both render in the Ratios tab as multiples. Two Codex EL1-review fixes landed: (1) debt_to_equity dedups current debt by value (LITE tags one amount as both ShortTermBorrowings and LongTermDebtCurrent — was double-counting; `dedup_numerator_by_value`, CC `3705473`); (2) the chart now plots multiple ratios as "2.19x" on a separate axis from margin % via `chartGroupOf(unit, uniAccount)` (AI `c894fec`). **EL1 / Phase A closed — Codex functional review converged** (cash_ratio + EL1 + both fixes). Tests: scripts 269 / prototype 30, 0 regression. Still pending: BVPS (needs period-end shares — we only have weighted-average), B (Quick Ratio + FCF), C (EBITDA), D (EL2 cross-period engine: ROE/ROA/turnover/YoY/Net-Debt-EBITDA — needs TTM/avg-balance storage contract decision), E (segment margin). The broaden-facts-wins-to-non-RATIO + pagination prerequisite for B/C is now **done** (see top entry).
- **Audit Metadata Schema v4 — Phase 6 closed** (2026-05-23, single round of in-session implementation).
  - **6.1 Preservation refactor**: pulled three copy-pasted `_preserve_*_cells` functions from `parse-10QK-gaap/xbrl_extract.py`, `parse-8k-nongaap/extract_8k_nongaap.py`, and `parse-SEC-supplement/extract_supplement_v3.py` into a single `_shared/preservation.py` (`preserve_audited_rows` + `values_match` + `row_has_audit` + `row_has_classification` + `PreservationResult` dataclass). Each parse skill now calls the shared helper with its own `identity_fn` (`build_preservation_identity` for GAAP/8K, `build_supplement_identity` for supplement) and prints the report. ~340 net lines removed, future matrix changes touch one file.
  - **6.2 Frontend audit indicator**: `StatementMatrix.tsx` displays "✓" badge on cells whose `provenance.audit_source` (canonical or legacy raw) is in `MANUAL_AUDIT_SOURCES`, or derived cells where `has_audited_inputs=true`. Classification-only cells get "·" badge. Tooltip extended with audit_source / audit_note / audited_by / audited_at / audit_evidence (source_doc + page_or_section) / preservation_event / new_extract_value_rejected / derived row's audited_input_cell_ids count. Both GAAP and Non-GAAP columns wired.
  - **6.3 DB legacy migration script**: `scripts/migrate_db_audit_source_v4.py` for one-shot normalize of pre-v4 enums in existing Supabase data. CLI dry-runs by default; `--apply` requires explicit flag; no-`--ticker` apply prompts `'YES'` confirmation. Two operations: `audit_normalize` (`MANUAL_AUDIT_FROM_PDF` / `MANUAL_AUDIT_FROM_8K_PDF` → canonical + `audit_source_raw` preserve) and `classification_promote` (`AGENT_CLASSIFIED` / `MANUAL_RECLASSIFIED` in audit_source → `classification_source`, mirrors Phase 2 F14 ADDED_BACK pattern). Per-row UPDATE (no batch) for granular failure diagnosis. 17 helper unit tests; **production data not yet migrated** — user runs per-ticker after frontend visual verification.
  - 32 new regression tests (15 preservation helper + 17 migration helper); full suite 258 passed.
- **Audit Metadata Schema v4 — Phase 5 closed** (2026-05-23, 3 rounds of Codex review).
  - `scripts/manual_edit.py` — ad-hoc audit edit CLI for one-off cell corrections outside `apply_audit.py` batch flow. Targets: `gaap` / `nongaap` / `supplement`. Operations: `stamp_new_row` / `stamp_existing_row` / `override_existing_audit` / `stamp_classification_existing_row`. Identity locate uses (period, uni_account, source_account, type, unit) for GAAP/8K and full dimensional tuple (via `build_supplement_identity`) for supplement; ambiguous match raises.
  - Strict channel boundary: `--audit-source` and `--classification-source` are mutually exclusive; classification mode is metadata-only (cannot change value, cannot create new numeric row without audit provenance); override of existing audit requires `--accept-new-values` and writes `accepted_new_value_replaces_audit` forensic.
  - Logger appends one JSON line per edit to `manual_edit_audit_log.jsonl` in the target dir; reads canonical + raw `audit_source` from the stamped row so legacy enum input (e.g. `MANUAL_AUDIT_FROM_PDF`) is recorded as both canonical and raw.
  - Adapter `_adapt_one_supplement_fact` now carries v4 channels (P5-F1); supplement dedupe (`adapt_supplement_facts`) merges v4 metadata across same-value duplicates (preferred chosen = the audited row), rejects when v4 metadata conflicts, and rejects when precision-dedupe would silently drop v4 metadata onto a row with a different numeric value (P5-F8 / P5-F10) — audit provenance never gets transplanted across values.
  - 44 regression tests covering all 3 targets, override forensic, classification-only safety, dimensional locate / no-match fail-closed, supplement dedupe v4 merge + conflict + precision boundary.
- **Audit Metadata Schema v4 — Phase 4 closed** (2026-05-22 → 2026-05-23, 2 rounds of Codex review).
  - `parse-SEC-supplement/extract_supplement_v3.py` now preserves dimensional audit/classification cells across re-extracts. Identity tuple `(period, period_kind, axis_key, member_key, uni_account, canonical_json(other_dimensions), unit)` per schema §6.2 — uses `_shared/dimensional_aliases.build_axis_key()` / `build_member_key()` (xbrl qname preferred, local-label fallback).
  - Behavior matrix mirrors GAAP/8K (MATCH / ADDED_BACK / CONFLICT / ACCEPT_NEW) with same audit vs classification-only branch, legacy `AGENT_CLASSIFIED` canonicalization on ADDED_BACK, and `DuplicateIdentityError` fail-closed.
  - Supplement-specific fail-closed mechanism: AUDIT conflicts (not classification-only) get written to `{TICKER}_supplement_conflict.json` with `audit_conflicts_unresolved=true` flag (schema §5 supplement clause). User reviews each conflict, then re-runs extract with `--accept-new-values` to drop preserved audit metadata.
  - Stale conflict.json is cleaned on any rerun that produces no audit conflicts (not just `--accept-new-values`).
  - Corrupt existing facts JSON raises `ValueError` rather than silently dropping prior audit metadata.
  - `_values_match` uses `≤ tol` per schema §5 wording (exact-boundary equality counts as MATCH).
  - 23 regression tests for Phase 4 (identity tuple variants, full preservation matrix, classification-only / mixed conflicts, duplicate fail-closed, corrupt JSON, tolerance boundaries, stale cleanup).
- **Audit Metadata Schema v4 — Phase 2 + Phase 3 closed** (2026-05-21 → 2026-05-22, 11 rounds of Codex review).
  - Canonical contract: `docs/audit-metadata-schema.md` v4.1 — three semantic channels (audit / classification / preservation event), strict allowlists for each, legacy enum normalization, re-extract behavior matrix.
  - Shared helper: `Tools/research-tools/_shared/audit_metadata.py` — allowlist constants, predicates (`is_manual_audit_source` / `is_manual_classification_source`), write helpers (`stamp_audit_provenance` / `stamp_classification` with locator / accession_number enforcement), copy helpers (`copy_audit_provenance` / `copy_classification_metadata` / `set_preservation_event`), preservation identity builder (`build_preservation_identity` + `DuplicateIdentityError`), unit normalization (`normalize_unit_label` / `expected_unit_family` / `resolve_unit_for_uni_account` — recognizes `$ thousands` / `thousands of USD` / `%` / `per share` etc.).
  - Parse skills updated (`CC_Switch_Config` mirrored 4-way to `~/.claude` / `~/.codex` / `~/.cc-switch`):
    - `parse-sec-cross-check/scripts/apply_audit.py` — writes canonical `MANUAL_AUDIT_FROM_OFFICIAL_FILING` + raw dual-write + audit_evidence dict (source_doc / notebooklm_source_id / period_scope / pdf_label) + classification path for long-tail rows.
    - `parse-8k-nongaap/scripts/apply_audit.py` — same canonical write path; review-table unit canonicalized before adopting; family compatibility check vs `expected_unit_family(uni_account)`.
    - `parse-8k-nongaap/scripts/extract_8k_nongaap.py` — `is_audit_value_filled` rewritten header-aware (no longer treats NLM Non-GAAP numbers as filled audit values); `resolve_8k_unit` promoted to module-level routes through `normalize_unit_label` (removes `$`-substring bug that mapped `$ thousands` to `USD_millions`); ADDED_BACK canonicalizes legacy `audit_source=AGENT_CLASSIFIED` to `classification_source`.
    - `parse-10QK-gaap/scripts/xbrl_extract.py` — same v4 preservation matrix (MATCH / ADDED_BACK / CONFLICT / ACCEPT_NEW with audit vs classification-only branches); duplicate identity fail-closed via `DuplicateIdentityError`.
  - Adapter / upsert / derive-base (`Tools/research-tools/_shared/sec_json_adapter.py`, `scripts/upsert_sec_financials.py`, `CC_Switch_Config/skills/derive-base/scripts/*`):
    - `_carry_audit_metadata_to_provenance` — v4 three-channel allowlist-guarded carry-through. Audit channel writes only allow MANUAL_AUDIT_SOURCES; legacy `AGENT_CLASSIFIED` in `audit_source` field auto-promotes to `classification_source`; invalid enum or orphan audit detail raises `ValueError` → row goes to `rejected` list; classification channel and preservation event channel also strict allowlist.
    - `derive_types.input_dict_from_fact` — carries `audit_source` / `audit_source_raw` / `audit_evidence` from FactRow provenance for derive-base lineage.
    - `audit.to_derived_metric_row` — computes `has_audited_inputs` + `audited_input_cell_ids` so derived rows declare audit lineage back to source cells.
    - `rules_q4._concepts_match` — upgraded from truthy `audit_source` check to `is_manual_audit_source` predicate; classification rows no longer falsely trigger Q4 concept relaxation.
  - Non-GAAP adapter: hardcoded `audit_source="NotebookLM_PDF_read"` removed (it was never a v4 audit source); replaced with `provenance.data_source` so it doesn't pollute audit predicate.
  - 156 regression tests, full suite passing. Test split: 62 helper / 17 10QK preservation / 4 8K preservation / 22 8K parse / 34 adapter+derive integration / 17 misc.
- **Phase 3 vendor-grade SEC parse pipeline merged to production** (CC_Switch_Config `0e2d9ef`). `parse-10QK-gaap` and `parse-SEC-supplement` now produce vendor-grade separated outputs using all four XBRL linkbases (cal / pre / lab / def) plus the raw instance document, alongside the existing inline `{T}_gaap.json` (which `parse-sec-cross-check` still reads).
  - `parse-10QK-gaap` adds three scripts: `full_linkbase.py` (fetches `_cal.xml` / `_pre.xml` / `_lab.xml`, emits `_gaap_edges_cal.json` / `_gaap_edges_pre.json` / `_gaap_labels.json` / `_sign_flip_concepts.json`), `build_separated.py` (orchestrates inline → separated facts + injects long-tail roll-up edges into cal), `cal_sum_sanity.py` (validates Σ(child × weight) = parent against companyfacts API, per-role to avoid duplicate-role double-count).
  - `parse-SEC-supplement` switches default flow from NLM-primary to XBRL-primary: `parse_def_xml.py` (Definition Linkbase → canonical axis / domain / member hierarchy per filing role), `parse_instance_xbrl.py` (instance doc → raw dimensional facts, period-filtered to single quarter 60-100d or FY 350-380d, prior-year / YTD discarded), `extract_supplement_v3.py` (def + instance + parse-10QK-gaap labels + legacy NLM validator → facts_v3 + edges_v3 + validation.md). NLM-only fallback workflow retained for cases XBRL lacks dimensional tags (small filers, carve-out periods).
  - End-to-end verified on INTC (5 periods) / AAOI (13) / SNDK (4): cross-check 22/22 ticker-periods 100% pass (393 rows, 0 ❌, 0 ⚠), cal sum sanity 204 ✅ / 0 ❌ / 86 partial / 26 skipped, AAOI XBRL ↔ NLM 3 ✅ / 0 ❌ / 106 XBRL-only (XBRL is more complete than NLM-derived).
- Parsed `AAOI` (Applied Optoelectronics, CIK 0001158114) from `Q1_FY2023` to `Q1_FY2026` end-to-end. GAAP via `parse-10QK-gaap` (IS 273 rows / BS 357 / CF 220), cross-check via `parse-sec-cross-check` against NotebookLM at 215 ✅ + 18 sign-flipped / 0 ❌ / 0 N/A, Non-GAAP via `parse-8k-nongaap` (6 spine metrics × 10 periods = 55 rows including `adjusted_ebitda`).
- Small-cap parse support hardened in three SEC skills (exposed by AAOI, which prints in thousands and disaggregates SG&A):
  - `parse-10QK-gaap` now auto-detects per-ticker USD reporting scale (`infer_usd_scale`: max revenue ≥ $1B → `USD_millions`, else `USD_thousands`). Each row carries its own `unit` field; downstream consumers should never assume a default scale.
  - `parse-10QK-gaap` SG&A composite fallback: when filer has no `SellingGeneralAndAdministrativeExpense` tag, sum the standard us-gaap sub-tags into the core key AND preserve sub-values as `operating_expense_long_tail` rows (`rolls_up_to=selling_general_administrative`) so granularity isn't lost.
  - `parse-sec-cross-check` reconciles NLM ↔ SEC scales using the SEC row's `unit` (AAOI thousands ↔ thousands and INTC millions ↔ millions both compare 1:1). Adds Basic/Diluted disambig by unit and aggregates `Sales and Marketing` + `General and Administrative` for composite SG&A comparison.
  - `parse-8k-nongaap` adds `adjusted_ebitda` to the canonical Non-GAAP metric keys (small-cap spine metric).
- INTC regression validated (95 ✅ / 0 ❌ / 0 N/A unchanged after structural fixes).
- Renamed the US SEC skill to `parse-sec-filing` so it now mirrors `parse-twse-ixbrl` naming.
- Replaced the US SEC skill's old export-oriented workflow with a direct Supabase ingestion flow via `Tools/research-tools/parse-sec-filing/batch_parse.py`.
- `parse-sec-filing` now canonicalizes US XBRL output into `Financials Viewer` schema keys and writes directly to `financial_companies`, `financial_facts`, and `financial_metrics`, with JSON retained only as an audit artifact.
- Live-smoke-tested the new `parse-sec-filing` ingestion on `INTC`; Supabase now contains canonical `INTC` rows in all three tables.
- Supplemented `INTC` business-segment revenue and operating income rows in `financial_supplement` from NotebookLM (`INTC - Intel Corporation - Official Materials`) for `Q1_FY2024` to `Q3_FY2025`, direct `FY2024` / `FY2025` annual values, and `Q1_FY2026`, storing only non-overlapping leaf business segments (`CCG`, `DCAI`, `Intel Foundry`, `All Other`) so `Financials Viewer` segment totals remain meaningful.
- Expanded the US SEC ingestion/display path so filing-native Intel income-statement rows such as restructuring charges, intangible amortization, gains/losses on equity investments, non-controlling-interest income, and weighted-average shares can flow through `parse-sec-filing` into `Financials Viewer`; `INTC` also now uses ticker-specific filing-style income-statement labels plus synthetic display rows for `Operating expenses` and total `Net income (loss)`.
- Taiwan direct `FY` annual period-based values are now stored as direct `FYyyyy` rows in `financial_facts` with annual-direct provenance, while quarterly views stay clean by filtering periods instead of hiding those rows in a side path.
- Fixed `Financials Viewer` annual aggregation so direct `FYxxxx` disclosures are preserved and preferred over quarterly fallback aggregation, which restores correct annual segment / geography display for Taiwan supplement data such as `2308 台達電`.
- Re-parsed `2454 聯發科` local TWSE MOPS iXBRL files so stale `Q4_FY2025` annual EPS values in quarterly storage were replaced with reconstructed single-quarter EPS. Taiwan `Q4 EPS` now prefers `FY - 9M cumulative EPS`, and only falls back to `FY - (Q1 + Q2 + Q3)` if cumulative EPS is unavailable.
- `Financials Viewer` tables now support cell-level provenance markers so annual-direct rows and reconstructed `Q4 EPS` can show a hover note on the specific cell.
- Added `2308 台達電` to `Financials Viewer`.
- Parsed local TWSE MOPS iXBRL files for `2308` from `Q1_FY2021` to `Q4_FY2025` into Supabase via the existing `parse-twse-ixbrl` workflow.
- Supplemented `2308` segment revenue from NotebookLM (`2308 - 台達電 - 財報`) for `Q1_FY2024` to `Q3_FY2025` plus direct `FY2024` / `FY2025` annual values, and supplemented annual geography revenue for `FY2024` / `FY2025` in `financial_supplement`.
- Added derived `2308` `Q4_FY2024` / `Q4_FY2025` segment revenue rows to `financial_supplement` using `FY - (Q1 + Q2 + Q3)` with explicit source `DERIVED_Q4_SEGMENT_FROM_FY_MINUS_Q1_Q2_Q3`.
- Supplemented `2308` business-segment profit rows from NotebookLM for `Q1_FY2024` to `Q3_FY2025` plus direct `FY2024` / `FY2025` annual values, and added derived `Q4_FY2024` / `Q4_FY2025` segment profit rows using `FY - (Q1 + Q2 + Q3)` with explicit source `DERIVED_Q4_SEGMENT_PROFIT_FROM_FY_MINUS_Q1_Q2_Q3`. `Financials Viewer` / `SegmentTable` now expose this as `profit_by_business` and suppress the pie chart when negative segment values are present.
- Added a `Business P&L` card to `Financials Viewer` `Segments` tab for business segments. It shows Revenue, derived Cost, Profit, and derived Margin for each segment using the existing `revenue_by_business` / `profit_by_business` series.
- Added `7769 鴻勁` to `Financials Viewer`.
- Parsed local TWSE MOPS iXBRL files for `Q1_FY2025` to `Q4_FY2025` plus `Q4_FY2024` into Supabase.
- Updated the TWSE parser so it auto-upserts a minimal `financial_companies` row before writing `financial_facts`, preventing new-ticker foreign key failures.
- Removed invalid `Q4_FY2024` `IS/CF` rows for `7769` because `Q3_FY2024` is not present locally.
- Updated the TWSE parser so missing prior-period files now cause incomplete `IS/CF` rows to be dropped instead of writing YTD/full-year values into single-quarter periods.
- Supplemented `7769` weighted average basic/diluted shares in `financial_supplement` from NotebookLM MCP (`7769 - 鴻勁 - 財報`), using quarter values for `Q1` to `Q3` and annual values for `Q4`.
- Added `7769` `Q4_FY2025` derived weighted-average basic/diluted shares to `financial_metrics`, and updated `Financials Viewer` quarterly income-statement rendering to read those `derived` metrics instead of computing Q4 shares in the browser at render time.
- Added `7769` annual product-segment revenue rows from NotebookLM MCP into `financial_supplement` for `FY2024` and `FY2025`, and updated the segment views so direct `FY` disclosures show only in Annual mode. The consolidated filings do not disclose geography revenue splits or quarterly segment revenue splits.
- Added ticker-specific label overrides for `7769` in `Financials Viewer` so Balance Sheet and Cash Flow rows use the Chinese wording from the company’s consolidated reports rather than generic TIFRS labels.
- Added `docs/financials-data-rules.md` as the formal authority for financial statement storage/display rules, and linked `AGENTS.md` / `CLAUDE.md` to it.
- Updated the TWSE parser so Taiwan `Q4` EPS (`basic_eps` / `diluted_eps`) are reconstructed as single-quarter values via `FY - Q1 - Q2 - Q3` when all required quarter EPS values exist, instead of storing the annual `Q4` filing disclosure directly in quarterly facts.
- Updated annual aggregation rules so annual EPS is built from four single-quarter EPS values, while quarterly mode still blocks annual-only weighted-average share counts from being shown in `Q4`.
- Clarified the documentation split:
  - `docs/financials-data-rules.md` governs storage/derivation/display behavior
  - `docs/financials-view-schema.md` is the `key -> meaning/source` metric dictionary

## Next Suggested Steps
- **Phase 6.2 audit indicator visual verify — DONE** (2026-05-29). Confirmed in dev server: LITE `Pretax Income` cells render "✓" badge + tooltip (`source` + `audit`). AAOI/INTC have no manually-audited cells so no badge (expected).
- **Phase 6.3 DB legacy enum migration — DONE** (2026-05-29). Ran `migrate_db_audit_source_v4.py`: LITE 24 `sec_financial_facts` rows normalized `MANUAL_AUDIT_FROM_PDF` → `MANUAL_AUDIT_FROM_OFFICIAL_FILING` (raw preserved); AAOI/INTC/SNDK + full-table scan all 0 legacy rows. Frontend tooltip now shows canonical value. Audit schema v4 fully complete end-to-end including production data.
- Add lightweight regression coverage for `/api/valuation/[ticker]`.
- Decide whether valuation should stay Yahoo-based or move to a first-party normalized market-data pipeline.
- Create a small architecture note for portal module boundaries if feature count keeps growing.
